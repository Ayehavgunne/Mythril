#ifndef MYTHRIL_UTIL_H
#define MYTHRIL_UTIL_H
#include <algorithm>
using namespace std;

//template<class Container, class Element>
//bool in(const Container &container, const Element &element);
//bool in(const string &str, char a_char);

#endif
