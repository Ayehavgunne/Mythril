[project]
name = "mythril"
version = "0.1.0"
description = "My programming language. For fun and not profit."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "prettyprinter>=0.18.0",
]

[dependency-groups]
dev = [
    "pyright>=1.1.411",
    "ruff>=0.16.3",
]
